import { PlaceLetterOnBoardCorner } from './place-letter-on-board-corner';

describe('PlaceLetterOnBoardCorner', () => {
    it('should create an instance', () => {
        expect(new PlaceLetterOnBoardCorner()).toBeTruthy();
    });
});
